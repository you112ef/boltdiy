{
	"name": "boltdiy",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@8.15.0",
	"scripts": {
		"build": "mkdir -p build/client && cp -r public/* build/client/",
		"dev": "python3 -m http.server 5173 --directory public",
		"start": "python3 -m http.server 8080 --directory build/client",
		"deploy": "pnpm run build",
		"preview": "pnpm run build && pnpm run start"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.1",
		"agents": "^0.0.94",
		"zod": "^3.25.51"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"typescript": "^5.8.3"
	}
}
